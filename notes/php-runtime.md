Разница CLI vs web — не «где», а «как приходит вход»:
- CLI — вход через команду и аргументы, HTTP нет.
- web — вход через HTTP-запрос (method/headers/body), ответ — со status code.

php -S` - учебный сервер, а не production-схема.